import React from 'react'
import './Header.css'

const Header = () => {
    return(
        <div className='header'>This is the header, and this is how I set up my components, so that I can see if they're working before I do anything else.</div>
    )
}

export default Header